
# הגדלת לוגואות Apple Pay ו-Google Pay

## שינוי בודד

### קובץ: `src/pages/Checkout.tsx`

שינוי גודל ה-SVG של שני הלוגואות מ-`h-8` (32px) ל-`h-12` (48px) כדי שיהיו קריאים וברורים בתוך הכפתורים.

- **ApplePayLogo** (שורה 58): `className="h-8"` ישתנה ל-`className="h-12"`
- **GooglePayLogo** (שורה 66): `className="h-8"` ישתנה ל-`className="h-12"`
